/**
 * Zustand 状态管理导出
 *
 * 本目录存放所有 Zustand store，用于全局状态管理。
 *
 * Store 列表 (待实现):
 * - videoStore.js - 视频状态管理
 * - analysisStore.js - 分析结果状态管理
 * - generationStore.js - 生成任务状态管理
 * - uiStore.js - UI 状态管理 (模态框、通知等)
 *
 * 使用示例:
 * import { useVideoStore, useAnalysisStore } from '../store/index.js';
 *
 * function MyComponent() {
 *   const { videos, addVideo } = useVideoStore();
 *   return <div>{videos.length} videos</div>;
 * }
 */

export {};
